import React from 'react';
import ReactDOM from 'react-dom/client';
import OverlayDisplay from './src/components/OverlayDisplay';
import './src/styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <OverlayDisplay />
  </React.StrictMode>
);
